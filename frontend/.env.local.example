# Copy to .env.local in this folder (frontend/) if you need to override the API URL.
# Local default in code is http://127.0.0.1:8000/api
# On Vercel, set NEXT_PUBLIC_API_BASE_URL in the project Environment Variables instead.

NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api
